# Instagram Comment Analyzer v10.3

- Google Sheets: writes strictly after the absolute last non-empty row. Existing rows are never targeted.
- Instagram login: click the username/password field in the remote Instagram screen, then type or paste directly. No separate bottom text field/button.
- Monorepo Railway Dockerfile: use Dockerfile Path `/worker/Dockerfile` with repository-root build context.

Deploy:
- Vercel Root Directory: `vercel`
- Railway Dockerfile Path: `/worker/Dockerfile`
- Railway Volume: `/data`
